"""Artifact module.

Artifacts are the outputs produced throughout an investigation's
lifecycle — execution plans, paper collections, reports, etc.
Each artifact is versioned and carries a JSONB payload whose
structure depends on the artifact type.
"""
