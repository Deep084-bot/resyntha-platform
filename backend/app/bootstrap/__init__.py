"""Application bootstrap package.

Handles the startup and shutdown lifecycle of the Resyntha platform.
All initialization and teardown logic lives here rather than in
FastAPI lifespan or module-level code.
"""
